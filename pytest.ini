[pytest]
testpaths =
    tests
pythonpath = .
addopts = --slowmo 500 --browser chromium --tracing retain-on-failure --video=retain-on-failure --screenshot=only-on-failure --timeout 60 --html=report.html -s -q -n auto