[pytest]
testpaths =
    tests
pythonpath = .
addopts = --browser chromium --tracing retain-on-failure --video=retain-on-failure --screenshot=only-on-failure --timeout 60 --html=report.html -q -n auto